name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

# Cancel superseded runs on the same ref; the full matrix is expensive.
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

env:
  # The Node version used wherever the matrix does not apply.
  PRIMARY_NODE: 24

jobs:
  # plan.md 8: the gate commands run as one job so a phase cannot be closed
  # while any of them fails.
  checks:
    name: Checks (Node ${{ matrix.node }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        # 22 is the engines floor; 24 is what development runs on. Testing
        # only one of them leaves the other unverified.
        node: [22, 24]
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v7
        with:
          node-version: ${{ matrix.node }}
          cache: npm

      - run: npm ci

      - run: npm run typecheck

      - run: npm run lint

      - run: npm run format:check

      - run: npm run test

      - run: npm run build

      - name: Report bundle size
        # plan.md 3.10 asks for the initial entry gzip size to be recorded so
        # an unexpected jump is visible in review. One version is enough.
        if: matrix.node == env.PRIMARY_NODE
        run: |
          {
            echo "### Bundle size"
            echo
            echo '| File | Raw | Gzip |'
            echo '| --- | ---: | ---: |'
            for file in dist/assets/*.js dist/assets/*.css; do
              [ -e "$file" ] || continue
              raw=$(wc -c < "$file" | tr -d ' ')
              gzip=$(gzip -c "$file" | wc -c | tr -d ' ')
              echo "| $(basename "$file") | $((raw / 1024)) kB | $((gzip / 1024)) kB |"
            done
          } >> "$GITHUB_STEP_SUMMARY"

      # Uploaded once: artifact names must be unique across a matrix.
      - uses: actions/upload-artifact@v7
        if: matrix.node == env.PRIMARY_NODE
        with:
          name: dist
          path: dist
          retention-days: 7

  browser:
    name: Browser tests (${{ matrix.project }})
    runs-on: ubuntu-latest
    needs: checks
    strategy:
      fail-fast: false
      matrix:
        project: [chromium, firefox, webkit]
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v7
        with:
          node-version: ${{ env.PRIMARY_NODE }}
          cache: npm

      - run: npm ci

      - run: npx playwright install --with-deps ${{ matrix.project }}

      - run: npm run build

      - run: npm run test:e2e -- --project=${{ matrix.project }}

      - uses: actions/upload-artifact@v7
        if: failure()
        with:
          name: playwright-report-${{ matrix.project }}
          path: playwright-report
          retention-days: 7
